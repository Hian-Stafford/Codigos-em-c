#include <stdio.h>
#include <stdlib.h>

int main()
{
    //declaracao de variaveis
    int n, d;

    //interacao com o usuario
    printf("Digite um número: ");
    scanf("%d", &n);

    //estrutura de condicao
        if (n>0 && n%2==0)
        {
            //estrutura de loop
            for (d=0; d=n; n--)
            {
            printf("O número é positivo, par e a ordem decrescente é %d\n", n-1); //coloquei aqui menos 1 para chegar em 0 na ordem decrescente
            }
        }
        //contra condicao
        else
        {
            printf("Valor inválido\n"); //caso o valor seja impar ou seja negativo, retornará esse valor
        }

    return 0;
}
